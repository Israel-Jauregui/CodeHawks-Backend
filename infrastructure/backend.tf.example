# `backend.tf` declares an empty S3 backend. These values are passed by the
# protected workflow rather than committed to the repository:

bucket       = "REPLACE_WITH_BOOTSTRAP_STATE_BUCKET"
key          = "backend/production.tfstate"
region       = "us-east-1"
encrypt      = true
use_lockfile = true
